# TapNow 节点式AI视频创作工具开发计划

## 项目概述
创建一个类似 TapNow 的桌面应用，支持节点式工作流，可上传图片并通过连线连接到API节点（可灵、即梦等）生成视频。

## 核心功能

### 项目管理
- 新建项目时在指定磁盘的 `AIVIDEO` 文件夹下创建项目文件夹
- 主界面显示项目图标列表，点击进入对应项目
- 进入项目后才能进行节点编辑
- **新建项目对话框**: 深色主题，清晰可见
- **单实例限制**: 
  - 应用程序只能运行一个实例
  - 同一时间只能打开一个项目编辑器
  - 打开新项目时自动关闭当前项目

### 素材与存储
- 上传的图片自动复制到项目的 `素材库` 文件夹
- 生成的视频保存为 `节点名.MP4`，图片保存为 `节点名.PNG`
- 视频缩略图保存为 `节点名_thumb.jpg`

### 节点编辑
- 支持拖拽节点到画布
- 鼠标左键拖拽连线（从输出socket到输入socket）
- Delete键删除选中的连线或节点
- Ctrl+C/V 复制粘贴节点（包含连线）
- Ctrl+Z/Y 撤销重做
- **自动保存**: 拖入节点、删除节点、创建连线、粘贴节点后自动保存工作流
- **滚轮缩放**: 向上滚动放大，向下滚动缩小
- **Ctrl+点击**: 按住Ctrl键点击节点，全选该节点连接的所有节点（包括上游和下游，当前节点也会被选中）

### 连线规则
- **输入接口（input）**: 每个输入口只允许连接一条线（保证数据来源唯一）
- **输出接口（output）**: 允许连接多条线，支持一对多分发（如一个API节点连接多个视频输出节点）

### 工作流模板
- 预设常用工作流模板（基础图生视频、首尾帧过渡、双模型对比）
- **点击放置**: 选择模板后鼠标变为十字光标，点击画布任意位置放置模板
- **不清除现有内容**: 模板会添加到点击位置，不会清除现有节点
- **精确位置**: 左上角第一个节点精确位于鼠标点击位置
- 支持导入/导出工作流JSON

### 复制粘贴功能
- **复制**: 选中节点后按Ctrl+C，复制节点及其之间的连线
- **粘贴**: 按Ctrl+V粘贴到鼠标位置，保持节点相对位置
- **自动命名**: 粘贴的视频输出节点自动分配唯一名称

### 右键菜单执行
- **视频输出节点右键菜单**: 点击"执行工作流"执行当前分支
- 只执行连接到该输出节点的上游节点
- 支持多分支独立执行

### 执行与任务队列
- **并行执行**: 支持多个工作流同时执行（生图、即梦生视频、可灵生视频可同时进行）
- **每个 OutputNode 独立线程**: 每个输出节点对应一个独立的执行线程，互不干扰
- **可不断添加**: 执行中可继续提交新任务，同一输出节点不会重复提交
- **API并发限制**: 各API自带限流重试机制，超过并发限制时自动等待重试
- **队列查看器**: 工具栏“📋 队列”按钮打开任务队列对话框
  - 显示任务名称（输出节点名）、类型（可灵生视频/即梦生视频/香蕉生图）、状态、进度消息
  - 支持停止单个任务
  - 支持全部停止
  - 支持清除已完成/失败的任务
  - 实时更新状态（排队中/执行中/完成/失败/已取消）
- **工具栏按钮**: 队列按钮显示当前运行任务数 "📋 队列(3)"
- **状态栏**: 显示当前执行中的任务数，全部完成时显示“✅ 所有任务已完成”

### 视频(图片)输出节点
- 拖入时自动分配唯一名称（output_1, output_2...）
- **名称不重复**: 无论拖入、模板放置还是粘贴，序号都不会重复
- **智能双模式**: 自动检测上游节点类型，决定输出图片或视频
  - 连接香蕉生图节点 → 输出 `.png` 图片
  - 连接可灵/即梦节点 → 输出 `.mp4` 视频
- 右键菜单支持执行工作流和打开文件所在文件夹
- 文件保存为 `{输出名称}.mp4` 或 `{输出名称}.png`
- **预览区域**: 生成后显示缩略图
  - 图片: 点击/双击均用系统默认图片查看器打开
  - 视频: 点击在节点内 OpenCV 播放，双击用系统播放器打开
- **自动检测已有文件**: 加载项目时根据输出名称自动扫描 .mp4 和 .png 文件
- **名称变更自动检测**: 修改输出名称后自动检测新名称对应的文件
- **自动生成缩略图**: 如果找到 mp4 但没有缩略图，使用 OpenCV 从视频第一帧自动提取

### 视频预览（OpenCV 方案）
- 视频生成后节点显示第一帧缩略图，图片生成后直接显示图片
- **点击播放**: 视频使用 OpenCV 逐帧渲染到 QLabel，不依赖系统编解码器
- **图片单击**: 直接用系统默认图片查看器打开
- **再次点击暂停**: 暂停视频播放并恢复缩略图显示
- **播放结束自动复位**: 播放完毕自动恢复缩略图
- **无缩略图降级**: 如果无法提取缩略图，显示"▶ 点击播放"文字按钮

### 右键菜单功能
- **图片上传节点右键菜单**:
  - "📁 打开所在文件夹" - 打开图片所在文件夹并选中文件
  - "📋 复制图片" - 将图片数据复制到系统剪贴板，可直接在其他软件中粘贴为图片
  - "🎨 绘制蒙版" / "🎨 重新绘制蒙版" - 弹出蒙版绘制面板
    - 背景显示原图轮廓，用户可看清图片内容
    - 左键涂抹：涂抹区域显示红色高亮，对应蒙版像素值=255
    - 右键擦除：擦除已涂抹区域
    - 支持调整画笔大小并切换橡皮擦模式
    - 重新绘制时保留之前的涂抹区域
    - 确认后图片上传节点新增一个蒙版输出口，输出灰度蒙版图片
    - 蒙版文件保存为 `{图片名}_mask.png`，与原图同目录
    - 复制粘贴节点后蒙版数据和双口均一并保留
- **视频(图片)输出节点右键菜单**: 
  - "执行工作流" - 执行当前分支
  - "打开文件所在文件夹" - 打开输出文件所在文件夹并选中文件（仅文件存在时显示）

### API集成
- 支持可灵AI、即梦AI、Gemini (香蕉模型)、Google Veo 等API
- API密钥加密存储
- 支持测试连接验证
- **图片自动压缩**: 执行任务前自动检查输入图片大小
  - 如果图片超过 4.7MB，自动压缩到 4.7MB 以内
  - 适用于可灵生视频、即梦生视频、香蕉生图、Veo生视频
  - 压缩使用 JPEG 格式，质量从 95 逐步降低直到满足大小限制
  - 透明图片自动转换为 RGB 格式
- **即梦AI签名**: 使用 HMAC-SHA256 签名，包含 content-type、host、x-date 三个签名头
- **即梦API端点**: Submit=CVSync2AsyncSubmitTask, Query=CVSync2AsyncGetResult, Version=2022-08-31
- **Gemini API**: 使用 REST API + API Key 认证，base64 内联图片数据
- **Gemini 中转地址**: 支持自定义 BASE_URL 中转服务，留空则直连官方 API
- **Gemini 限流处理**: HTTP 429 自动重试（最多6次，间隔递增30s）
- **Gemini 网络韧性**: SSL/连接/超时错误自动重试（最多6次，间隔递增5s）
- **Veo3 API**: 向量引擎中转服务，支持中文提示词自动转英文
  - 支持多种 Veo 模型：veo2/veo3 系列
  - 支持图生视频、首尾帧、视频元素等模式
- **错误识别**: 
  - code=50400 "Access Denied" → 提示用户开通即梦AI服务或添加 IAM 权限
  - AccessDenied → 提示添加 CVImageProcessFullAccess 策略
  - SignatureDoesNotMatch → 密钥错误

### 模型选项
- 可灵AI: 多模型(kling-v3-omni/v3/video-o1/v2-6/v2-5-turbo/v2-master/v2-1-master/v2-1/v2/v1-6/v1-5/v1)、时长(v3系列3-15s/其他5s/10s)、分辨率(720p/1080p)、模式(std/pro)、CFG强度
  - 首尾帧支持按模型和模式动态控制
  - kling-v3, kling-v3-omni, kling-video-o1 支持图生视频和首尾帧（std/pro均可）
  - kling-v3, kling-v3-omni 支持3-15秒时长
- 即梦AI: 多模型(jimeng_v1/v2/v30/v30_pro)、时长(2-10s)、帧率24fps、seed
- Gemini (香蕉模型): 图片生成，支持文生图和图生图
  - gemini-2.5-flash-preview-image-generation (快速)
  - gemini-2.0-flash-preview-image-generation (快速)
  - gemini-3-pro-image-preview (专业版: 支持14张参考图, 4K输出, 思维链)
  - 宽高比: 1:1, 2:3, 3:2, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9
  - 分辨率: 1024(1K) / 2048(2K) / 4096(4K)，仅 Pro 模型支持，默认 2K
  - 最多 14 张参考图片输入（动态输入口 0-14）
- Google Veo (向量引擎中转): 视频生成，支持中文提示词自动转英文
  - veo_3_1 (默认，自适应首尾帧/文生视频)
  - veo3.1, veo3.1-fast, veo3.1-pro, veo3.1-4k, veo3.1-pro-4k
  - veo3-pro, veo3-fast, veo3-fast-frames, veo3-frames, veo3-pro-frames
  - veo2-pro, veo2-fast, veo2-fast-frames
  - veo2-fast-components, veo2-pro-components (视频元素)
  - 宽高比: 16:9, 9:16 (veo3/veo3.1 支持)
  - 所有视频均为无声版本
  - 支持中文提示词自动转英文 (enhance_prompt)
  - 支持视频超分 (enable_upsample)

### 视频生成模式
- **图生视频**: 单张图片生成视频（1个输入口）
- **首尾帧**: 首帧+尾帧图片生成过渡视频（2个输入口）
  - 第一个输入口：首帧图片
  - 第二个输入口：尾帧图片
  - 即梦v30首尾帧使用 req_key=jimeng_i2v_first_tail_v30，两张图片放入 binary_data_base64 数组
- 根据选择的模式动态调整输入口数量

### 批量处理
- 支持批量上传图片
- 队列管理和进度显示

### 生成历史
- 记录每次生成的参数和结果
- 支持重新加载历史配置

## UI设计
- **背景色**: 深黑色 (#1a1a1a)
- **字体**: Microsoft YaHei, Segoe UI, sans-serif
- **文字颜色**: 柔和白色 (#e0e0e0)
- **节点样式**: 圆角矩形，深色渐变背景
- **Socket样式**: 
  - 输入socket: 绿色圆点（左侧）
  - 输出socket: 蓝色圆点（右侧）
- **对话框**: 所有对话框使用深色主题
- **使用说明面板**: 
  - 位于编辑器右侧，宽度200px
  - 内容从 `resources/sm.txt` 读取
  - 每行自动添加序号
  - 可在API设置的通用设置中开启/关闭
  - 设置保存后立即生效

## 技术栈
- **GUI框架**: PyQt5
- **节点编辑器**: 自定义实现
- **视频播放**: OpenCV 逐帧渲染（避免 Windows DirectShow 编解码器兼容问题）
- **API集成**: 可灵AI API、即梦AI API（火山引擎 HMAC-SHA256 签名）
- **打包工具**: PyInstaller

## 项目结构
```
tapnow/
├── main.py                 # 程序入口
├── PLAN.md                 # 开发计划
├── core/
│   ├── node_editor/        # 节点编辑器核心
│   │   ├── scene.py
│   │   ├── graphics_view.py
│   │   ├── node_item.py
│   │   ├── edge.py
│   │   └── undo_stack.py   # 撤销重做
│   └── nodes/              # 各类节点
│       └── nodes.py        # ImageNode, KlingAPINode, JimengAPINode, GeminiAPINode, VeoAPINode, ImageEditNode, VideoNode, OutputNode
├── api/
│   ├── base_api.py
│   ├── kling_api.py
│   ├── jimeng_api.py       # 火山引擎签名 + CVSync2AsyncSubmitTask
│   ├── gemini_api.py       # Google Gemini REST API + base64 内联图片
│   ├── veo_api.py          # Google Veo 官方 API (已弃用)
│   └── veo3_api.py         # 向量引擎 Veo3 中转 API
├── ui/
│   ├── main_window.py      # 主窗口（项目列表）
│   ├── project_widget.py   # 项目图标组件
│   ├── editor_window.py    # 项目编辑器窗口（含执行线程、状态栏）
│   ├── task_queue.py       # 任务队列管理器 + 队列查看器对话框
│   ├── project_dialog.py   # 新建项目对话框
│   ├── api_settings_dialog.py
│   └── node_panel.py
├── utils/
│   ├── config.py
│   ├── project_manager.py
│   └── file_utils.py
├── resources/
│   ├── sm.txt              # 使用说明内容
│   └── templates/          # 工作流模板
├── requirements.txt
└── build.spec
```

## 文件夹结构
```
D:\AIVIDEO\
└── {项目名}\
    ├── 素材库\              # 上传的图片存放
    ├── workflows\           # 工作流配置
    ├── history.json         # 生成历史
    ├── {节点名}.mp4        # 生成的视频
    ├── {节点名}.png        # 生成的图片（来自香蕉模型）
    └── {节点名}_thumb.jpg  # 视频缩略图
```

## 开发步骤

### 第一阶段：基础框架重构 ✅
1. 重构主窗口为项目列表界面
2. 创建项目图标组件
3. 创建项目编辑器窗口
4. 实现项目进入/退出逻辑

### 第二阶段：节点编辑器增强 ✅
5. 实现撤销/重做功能
6. 实现节点复制/粘贴
7. 完善连线拖拽逻辑
8. 实现Delete删除连线
9. 输出口支持多连线，输入口限制单连线

### 第三阶段：素材与视频功能 ✅
9. 实现素材库功能（图片复制）
10. 实现视频预览（OpenCV逐帧渲染）
11. 实现视频缩略图显示
12. 添加模型/时长/分辨率/CFG选项
13. 实现首尾帧模式（动态输入口）
14. 视频输出节点自动检测已有视频

### 第四阶段：API集成与执行 ✅
15. 即梦AI签名实现（HMAC-SHA256）
16. 即梦API错误识别（50400/AccessDenied）
17. 可灵AI首尾帧模型能力矩阵
18. 执行状态横幅（蓝/绿/红）
19. 防重复执行机制

### 第五阶段：图片修改与链式执行 ✅
20. 图片修改节点 (可灵扩图/即梦超清/即梦局部重绘)
21. 视频上传节点
22. OutputNode 输出端口 + 链式执行
23. 输出节点复制粘贴为上传节点
24. 拖拽视频文件到画布

### 第六阶段：工作流与批量处理
25. 实现工作流模板
26. 实现批量生成
27. 实现生成历史记录

### 第七阶段：完善打包
28. UI优化和测试
29. PyInstaller打包exe

## 快捷键
| 快捷键 | 功能 |
|--------|------|
| Ctrl+Z | 撤销 |
| Ctrl+Y | 重做 |
| Ctrl+C | 复制节点（含连线） |
| Ctrl+V | 粘贴节点到鼠标位置 |
| Delete | 删除选中项 |
| Ctrl+S | 保存工作流 |
| Ctrl+点击 | 全选连接的上下游节点 |
| 鼠标滚轮 | 缩放画布 |
| 鼠标中键拖拽 | 平移画布 |
| Ctrl+左键点击 | 全选连接的所有节点 |

## 可灵AI模型选项
- 模型: kling-v3-omni, kling-v3, kling-video-o1, kling-v2-6, kling-v2-5-turbo, kling-v2-master, kling-v2-1-master, kling-v2-1, kling-v2, kling-v1-6, kling-v1-5, kling-v1
- 时长: 
  - kling-v3, kling-v3-omni: 3-15秒
  - 其他模型: 5秒, 10秒
- 分辨率: 720p(std标准), 1080p(pro专业)
- 生成模式: 图生视频, 首尾帧
- CFG Scale: 0.0-1.0 (默认0.5)

### 可灵首尾帧支持情况
| 模型 | std(720p) | pro(1080p) |
|------|-----------|------------|
| kling-v3-omni | ✅ | ✅ |
| kling-v3 | ✅ | ✅ |
| kling-video-o1 | ✅ | ✅ |
| kling-v1 | ❌ | ✅ |
| kling-v1-5 | ❌ | ✅ |
| kling-v1-6 | ❌ | ✅ |
| kling-v2 | ❌ | ❌ |
| kling-v2-master | ❌ | ❌ |
| kling-v2-1 | ❌ | ✅ |
| kling-v2-1-master | ❌ | ❌ |
| kling-v2-5-turbo | ❌ | ✅ |
| kling-v2-6 | ❌ | ✅ |

## 即梦AI模型选项
- 模型: jimeng_v1, jimeng_v2, jimeng_v30, jimeng_v30_pro
- 时长: 5秒, 10秒
- 帧率: 24fps (固定)
- 生成模式: 图生视频, 首尾帧 (jimeng_v30_pro 仅支持图生视频)
- Seed: -1(随机) 到 2147483647
### 图片修改节点 (ImageEditNode)
- 三种编辑模式：可灵扩图 / 即梦超清 / 即梦局部重绘
- **可灵扩图**: 上/下/左/右各0~2.0比例滑块，可选提示词，单图输入
  - API: POST `/v1/images/editing/expand`，总扩展面积 ≤ 3倍原图
- **即梦超清**: 分辨率(4k/8k)，锐化强度(0-100)，单图输入
  - API: `req_key=jimeng_i2i_seed3_tilesr_cvtob`
- **即梦局部重绘**: 种子(seed)，提示词（必填，≤120字符），双输入口(原图+遮罩)
  - 遮罩：单通道灰度图，黑色(0)=保留，白色(255)=重绘
  - API: `req_key=jimeng_image2image_dream_inpaint`
- 输入: 图片 (1~2个，根据模式动态调整)
- 输出: 图片 (1个，修改后的图片)

### 视频上传节点 (VideoNode)
- 上传视频文件（.mp4/.mov/.avi/.mkv/.wmv/.flv/.webm）
- 拖拽视频文件到画布自动创建
- 自动提取首帧作为缩略图预览（依赖 OpenCV）
- 输出: 视频 (1个)

### 链式执行
- **OutputNode 新增输出端口**: 输出节点右侧有蓝色输出 socket
- OutputNode 的输出可以连接到下游 API/图片修改节点
- 执行时自动沿链路追踪：Image → API → Output1 → API2 → Output2
- 下游节点可从上游 OutputNode 获取结果文件路径

### 输出节点复制粘贴
- OutputNode 右键菜单新增 "📋 复制输出文件路径"
- 复制后按 Ctrl+V 在鼠标位置创建图片上传或视频上传节点
- 自动识别文件类型：图片 → ImageNode，视频 → VideoNode
## 节点输入口说明
### 图片上传节点
- 输出: 图片 (1个，始终存在)
- 输出: 蒙版 (1个，可选，绘制蒙版后出现)

### 视频生成节点（可灵/即梦）
- 模式=图生视频: 输入=图片 (1个)
- 模式=首尾帧: 输入=首帧图片 + 尾帧图片 (2个)
- 输出: 视频 (1个)

### Veo生视频节点（向量引擎中转）
- 需要单独配置 Veo3 API 密钥（在设置 → Veo3视频）
- 支持中文提示词自动转英文
- 模式=图生视频: 输入=图片 (1个)
- 模式=首尾帧: 输入=首帧图片 + 尾帧图片 (2个)
- 输出: 视频 (1个)
- 模型: veo_3_1 (默认) / veo3.1 / veo3.1-fast / veo3.1-pro / veo3.1-4k / veo3.1-pro-4k
       veo3-pro / veo3-fast / veo3-fast-frames / veo3-frames / veo3-pro-frames
       veo2-pro / veo2-fast / veo2-fast-frames / veo2-fast-components / veo2-pro-components
- 宽高比: 16:9, 9:16 (veo3/veo3.1 支持)
- 中文转英文: 是/否 (enhance_prompt)
- 视频超分: 是/否 (enable_upsample)
- 注意: 所有视频均为无声版本

### 香蕉生图节点（Gemini）
- 输入: 参考图片 (1个橙色多连接输入口, 支持同时连接多个图片节点, 最多14条连线)
- 输出: 图片 (1个, 可连接到可灵/即梦生视频节点或视频输出节点)
- 使用方式: 多个图片上传节点的输出分别连到香蕉模型的同一个橙色输入点即可
- 模型: Flash(快速) / Pro(专业版, 支持4K和14张参考图)
- 分辨率: 仅 Pro 模型可选 1K/2K/4K
- 生成的图片保存到 `素材库/gemini_{timestamp}.png`
- 支持级联: 香蕉生图 → 可灵/即梦生视频 → 视频输出
- **参考图缩略图条**: 提示词上方显示已连接参考图的缩略图网格 (每行4张, 40×40px)
  - 鼠标拖拽可改变参考图顺序（拖到目标左侧=插到前面，右侧=插到后面）
  - 拖拽中源图橙色高亮、目标位置绿色高亮
  - 使用纯鼠标事件实现（非QDrag），兼容QGraphicsProxyWidget
  - 排序结果保存到工作流JSON，加载时自动恢复
  - 执行时按用户排序后的顺序传入参考图

### 视频(图片)输出节点
- 输入: 视频或图片 (1个)
- 输出: 结果 (1个，新增，用于链式执行传递结果到下游)
- **智能双模式输出**: 根据上游节点自动判断输出类型
  - 连接香蕉生图 (Gemini) → 输出图片 (.png)
  - 连接可灵/即梦生视频 → 输出视频 (.mp4)
- **双击行为**: 
  - 图片文件 → 用系统默认图片查看器打开
  - 视频文件 → 用系统默认播放器打开
- **单击缩略图**: 
  - 图片文件 → 用系统默认图片查看器打开
  - 视频文件 → OpenCV 节点内播放/暂停
- 自动命名: output_1, output_2...（序号不重复）
- 右键菜单: 执行工作流、打开文件所在文件夹
- **自动检测**: 加载时同时检测 .mp4 和 .png 文件
